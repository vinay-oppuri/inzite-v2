{
  "name": "services",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python -m uvicorn src.app.main:app --reload --host 127.0.0.1 --port 8000",
    "test": "uv run pytest -q"
  }
}
